{
  "name": "eeemail-desktop",
  "version": "0.1.0",
  "private": true,
  "license": "MPL-2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit",
    "tauri": "tauri",
    "build:demo": "tsc --noEmit && VITE_EEEMAIL_DEMO=1 vite build --outDir dist-demo",
    "preview:demo": "vite preview --outDir dist-demo --port 5174 --strictPort"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.1",
    "typescript": "^5.9.2",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.0"
  }
}
