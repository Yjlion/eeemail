{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The shell is one IPC pipe: a command in, an event stream out. `rpc_send`, `stage_attachment`, `first_run_pending` and `acknowledge_first_run` are app commands and need no permission here; `listen` is the core event plugin and does. Without this file the ACL resolves to nothing and every engine event is refused -- which is exactly what shipped in v0.3.0.",
  "windows": ["main"],
  "permissions": ["core:event:default"]
}
